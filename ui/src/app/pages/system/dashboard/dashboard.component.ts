import { Component, OnDestroy } from '@angular/core';

@Component({ selector: 'system-dashboard', styleUrls: ['./dashboard.component.scss'], templateUrl: './dashboard.component.html' })
export class SystemDashboardComponent implements OnDestroy {
    constructor() { }

    ngOnDestroy() {
    }
}
