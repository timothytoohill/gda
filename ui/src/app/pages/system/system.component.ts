import { Component, OnDestroy } from '@angular/core';

@Component({ selector: 'system', templateUrl: 'system.component.html', styleUrls: ['system.component.scss'] })
export class SystemComponent implements OnDestroy {
    constructor() { }

    ngOnDestroy() {
    }
}
