import { Component, OnDestroy } from '@angular/core';
import { NbThemeService, NbMediaBreakpoint, NbMediaBreakpointsService } from '@nebular/theme';

@Component({ selector: 'readme', styleUrls: ['./component.scss'], templateUrl: './component.html' })
export class ReadMeComponent implements OnDestroy {

    constructor() {
    }

    ngOnDestroy() {
    }
}
